name = "ai-repo-radar"
compatibility_date = "2024-04-01"
pages_build_output_dir = "."

# Cloudflare Pages — deployed to usefreetools.dev
# Deploy: npx wrangler pages deploy . --project-name ai-repo-radar
# Routes configured in Cloudflare Dashboard → Pages → Custom Domains

[vars]
SITE_URL = "https://usefreetools.dev"
SITE_NAME = "AI Repo Radar"
